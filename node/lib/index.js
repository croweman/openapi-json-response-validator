const { 
    assertThatResponseIsValid,
    initialise,
    initialised,
    initialisationErrored,
    validateResponse,
    dispose
} = require('./dist/response-validation')

module.exports = {
    assertThatResponseIsValid,
    initialise,
    initialised,
    initialisationErrored,
    validateResponse,
    dispose
}
